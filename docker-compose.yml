services:
  app:
    build: ./backend
    image: url-shortener-app:local
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env
    depends_on:
      mongo:
        condition: service_started
      redis:
        condition: service_started

  worker:
    build: ./backend
    image: url-shortener-worker:local
    restart: unless-stopped
    command: ["node", "./src/api/worker.js"]
    env_file:
      - ./backend/.env
    depends_on:
      mongo:
        condition: service_started
      redis:
        condition: service_started
    healthcheck:
      disable: true

  redis:
    image: redis:7
    restart: unless-stopped
    command: ["redis-server", "--save", "60", "1", "--appendonly", "yes"]
    volumes:
      - redis-data:/data

  mongo:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
    driver: local
  redis-data:
    driver: local
